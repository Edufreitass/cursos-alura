package aula7.exercicio9;

/**
 * Compila, roda e imprime A75. Cuidado ao compilar e rodar pois alguns caracteres
 * podem precisar ser escapados pelo seu shell ou bash (não cobrado na prova).
 */
public class $_o0o_$ {

    // Compila, roda e imprime A75
    public static void main(String[] args) {
        int $$ = 5;
        int __ = $$++;
        if (__ < ++$$ || __-- > $$)
            System.out.print("A");

        System.out.print($$);
        System.out.print(__);
    }
}
